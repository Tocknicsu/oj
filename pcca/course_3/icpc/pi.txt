給定一圖的關係(U,J)
現在要在點上面編號
規定對於編號相同的點
則其路徑上至少要有一點編號大於他們

策略:
點的數量10000個 不可能把圖建起來
觀察可以發現
可以維護一個(num, size)代表這張圖至少要編到幾號 以及 圖上點的數量
對於U操作可以得到(max(num1, num2), size1+size2)
對於J操作可以得到( min(num1+size2, num2+size1), size1+size2)

