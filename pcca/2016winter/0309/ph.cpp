#include <bits/stdc++.h>
using namespace std;
#define MAX 512
struct POINT{
    int x, y;
};
POINT P[MAX];
int main(){
    int n;
    scanf("%d", &n);
    for(int i = 0 ; i < n ; i++){
        
    }
    return 0;
}
