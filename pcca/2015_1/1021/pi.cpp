#include <bits/stdc++.h>
using namespace std;
struct F{
    double a, b, c;
    F(){}
    F(double _a, double _b, double _c) : a(_a), b(_b), c(_c){}
};

F f[128];
void Solve(){
    int n, m, a, b, c;
    scanf("%d%d", &n, &m);
    for(int i = 0 ; i < n ; i++){
        scanf("%d%d%d", &a, &b, &c);
    }



}

int main(){
    int n;
    scanf("%d", &n);
    while(n--)
        Solve();
    return 0;
}
