#include <bits/stdc++.h>
using namespace std;

int main(){
    int n;
    while(cin >> n){
        map<int, int> m;
        m[1] = 1;
        int ans = 1;
        vector<int> vec(1);
        cin >> vec[0];
        for(int i = 1 ; i < n ; i++){
            int tmp, p;
            cin >> tmp;
            if(tmp >= vec.back()){

            } else {

            }
        }

    }
}
